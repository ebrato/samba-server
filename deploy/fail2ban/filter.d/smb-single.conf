# Fail2ban filter for smb-single auth failures.
# Matches log lines emitted by smb.cpp: "[warn] auth failure ip=<addr>"

[INCLUDES]
before = common.conf

[Definition]
_daemon = smb-single
failregex = ^.*\[warn\] auth failure ip=<HOST>\s*$
ignoreregex =
